var components = {
	init: function() {
		// masonry.init();
		// gradient.init();
		// nanoajax.init();
		// wishlist.init();
		portfolioSlider.init();
		toggle.init();
	},
};

window.addEventListener('load', function(){
	components.init();
});
